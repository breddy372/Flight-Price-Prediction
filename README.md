The repository consists of a jupyter notebook and .py file.
Jupyter notebook consists of model creation and EDA. Run it to get the pickle file which acts as input for py file.
.py file runs FastAPI, run command py-m uvicorn app:FastAPI --reload.

Change the loaction of assets inside the .py file
